{
  "name": "arvyax-wellness-platform",
  "version": "1.0.0",
  "description": "Full-stack wellness session platform with authentication and auto-save",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["wellness", "sessions", "authentication", "auto-save"],
  "author": "Arvyax Intern",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
